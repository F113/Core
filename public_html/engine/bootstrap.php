<?phprequire_once(__DIR__ . "/../config.php");require_once(__DIR__ . "/Core.php");require_once(__DIR__ . "/Model.php");if (empty($db_settings)) Core::exception('Missed db_settings in config.php');if (empty($routes)) Core::exception('Missed routes in config.php');if (!trait_exists('Patterns')) Core::exception('Patterns trait not found in config.php');define('DB_SETTINGS', $db_settings);//if (!isset($_GET['route'])) $_GET['route'] = 'index';$route = $_GET['route'] ?? 'index';new Core($_GET['route'], $routes, $db);