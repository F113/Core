<?phprequire_once(__DIR__ . "/class.functions.php");require_once(__DIR__ . "/../config.php");if (!trait_exists('Patterns')) Functions::error('Patterns trait not found in config.php');if (empty($db)) Functions::error('Missed database attributes in config.php');if (empty($routes)) Functions::error('Missed routes in config.php');require_once(__DIR__ . "/class.core.php");require_once(__DIR__ . "/class.model.php");if (!isset($_GET['route'])) $_GET['route'] = 'index';new Core($_GET['route'], $db, $routes);